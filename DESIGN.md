# DESIGN.md — 云悦工作台（JoyCloud Image Press）

## 产品定位与意象

批量图片生成工作流的可视化工作台。不是 AI 玩具，是生产工具。

**意象锚点**：活字印刷车间的**印务台**——纸灰底、墨黑铅字、青色印墨、洋红校样笔。
一批提示词就是一次开印，成品图是刚下印机的样张。整页靠**字号、字距与横线**分层，
不靠卡片、投影、图标。参考气质：报纸版面 / 学术期刊 / 印厂工单。

**明确绕开的第一直觉**：商务浅色泛滥的"惨白底 + 高饱和亮蓝 + 厚重投影卡片"，一律禁止；
也禁止 AI 产品俗套的紫蓝渐变。

## Design Tokens

所有 Token 定义在 `src/app/globals.css` 的 `:root` 与 `@theme inline`，
业务组件只用 Tailwind 语义类（`bg-paper` / `text-quiet` / `border-rule` …），不写裸 hex。

### 色彩

| 用途 | 值 | Tailwind |
| --- | --- | --- |
| 页面底（纸灰） | `#F3F2F2` | `bg-paper` / `bg-background` |
| 抬起面（输入框、卡片底） | `#FBFAFA` | `bg-raised` |
| 下沉面（日志、图片底） | `#EAE9E9` | `bg-sunk` |
| 主文字（墨黑） | `#201E1D` | `text-foreground` |
| 次文字 | `#605D5D` | `text-muted-foreground` |
| 弱文字 | `#7D7979` | `text-quiet` |
| 更弱（占位、无值） | `#9B9797` | `text-faint` |
| 分割线 | `#D7D3D3` | `border-rule` |
| 行内细线 | `#E0DDDD` | `border-rule-soft` |
| 虚线（排队/空态） | `#BAB6B6` | `border-rule-dash` |
| 印墨青（主色/运行中） | `#0088B0` | `bg-signal` / `text-signal` |
| 深青（链接、正文态） | `#006786` | `text-teal-deep` |
| 浅青（进度槽） | `#CBEEFF` | `bg-teal-tint` |
| 极浅青（模板标签底） | `#E9F8FF` | `bg-teal-wash` |
| 校样洋红（失败/待办/危险） | `#D6006C` | `bg-rose` / `border-rose` |
| 深洋红（洋红文字） | `#AA0B56` | `text-rose-deep` |
| 浅洋红（超时进度槽） | `#FFDEE6` | `bg-rose-tint` |
| 极浅洋红（失败卡底） | `#FFF1F4` | `bg-rose-wash` |

**只有两个强调色**：印墨青 = 进行中 / 主动作 / 链接；校样洋红 = 失败 / 待办 / 破坏性动作。
没有绿色——成功态由**图片本身**表达，计数用墨黑。
禁止：紫蓝渐变、彩色发光描边、厚阴影。

### 字体

- **正文与数据：衬线** `Source Serif 4`，**自托管**：woff2 收在 `src/app/fonts/`，
  由 `layout.tsx` 的 `next/font/local` 出字并挂 `--font-source-serif` 到 `<html>`，
  `globals.css` 的 `--font-serif` 引用它后再回落 Georgia / 中文衬线栈
  - **不要改回 Google Fonts 的远程 `@import`**：那会被 Next 的 CSS 管道整条剥离，
    构建产物里不会有任何 gstatic 引用，页面静默回落到 Georgia/宋体（这个坑踩过一次）；
    `next/font/google` 也不合适——要求构建机能出网到 gstatic，国内构建不稳
  - Source Serif 4 **没有中文字形**，只作用于拉丁字母与数字（编号、计时、批次号、钢印），
    中文一律由 `Songti SC / Noto Serif CJK SC / Source Han Serif SC / serif` 接住
- **顶栏与弹窗标题：无衬线** `Helvetica Neue` + 系统中文栈（`font-sans`）——工单抬头用无衬线，正文用衬线
- **结局钢印与运行日志：等宽** `ui-monospace, Menlo…`（`font-mono`），只给 `TIMEOUT` / `FAILED` / `CANCELLED` 与日志行
- 一切数字（编号、计时、计数、延迟、批次号）加 `tabular-nums`，跨行对齐
- 小标题用 `text-xs uppercase tracking-[0.14em] text-quiet`（每条份数 / 示例模板 / 最近运行），
  进度行用 `tracking-[0.1em]`——**靠字距而不是字号做层级**

### 形状与线条

- **圆角一律 2px**（`--radius-sm/md/lg` 全部 2px）；胶囊只留给状态点
- **不用图标**：动作一律是文字按钮（`复制` / `删除` / `中断此张`），仅保留 `＋` 与 `▾` 两个字形
- 层次靠 1px 横线 + 底色差：顶栏下 1px 墨黑主线 + 1px `#D7D3D3` 副线，列表项 1px `#E0DDDD`
- 禁止投影、玻璃拟态、卡片套娃

### 动效

- 功能化 150ms `ease-out`（`transition-colors`），禁止弹性/漂浮
- `ink-pulse` 1.6s：运行中的信号点，opacity 1→.28 + scale 1→.8，像墨点被压下又抬起
- `press-run` 0.9s linear：批次进度条在跑时叠一层走纸斜纹（`.press-stripe`）
- `card-in` 0.28s：成品按 index stagger 入场（40ms 间隔，上限 24 张）
- `.halftone`：成品图 `grayscale(.35) contrast(1.15)` + 3px 网点 `multiply` 叠加，做出样张质感
  - **注意**：该 utility 自带 `position: relative`，且在 utilities 层里排在 Tailwind 之后，
    会盖掉同元素上的 `absolute`。用它包图片时要用 `size-full` 撑满容器，不要再叠 `absolute inset-0`
- 骨架态 `.shimmer-bg`：暖灰 shimmer，不刺眼

## 布局

整页**滚动**（不是内部分区滚动）：`min-h-dvh`、左右 40px 内边距（`lg:px-10`）、底部 60px。

- **顶栏**（`top-bar.tsx`，无衬线）：字标「云悦工作台 / JoyCloud Image Press」+ 上游状态点与**实测**延迟
  + 「操作员」选择器 → 1px 墨黑主线 → **铅字条**（批次号 / 输入模式与份数 / 可改并发与超时 / 出版日期）→ 1px 副线
  - 出版日期在 `useEffect` 里生成，避免服务端时区导致 hydration 失配
- **两栏栅格**：`400px 1fr`，间距 56px，`items-start`，上边距 30px；左栏 `sticky top-6`
- **左栏 = 生产区**（`prompt-panel.tsx`）：标题 + 清空 → 分步/统一分段 → 编号行列表（`max-h-[330px]` 内滚动）
  → 追加一条（虚线）→ 每条份数（含派发数）→ 示例模板（三个青底标签）→ 主按钮 + 取消 → 提示 → 最近运行
- **右栏 = 出料区**：批次抬头（`本批次 · N 张` + 五态计数 + 运行日志/重试全部失败/全部下载）
  → 2px 进度条 + `已完成 N% / 已耗时 · 预计剩余` → 日志面板（可折叠）→ 任务网格 → 一行斜体说明
- **任务网格**：`repeat(auto-fill, minmax(240px, 1fr))`，间距 24px，1:1 卡片
- 窄屏（`< lg`）：两栏堆叠为单列，网格按 240px 下限自然回落

### 隐藏后台（用量总账）

`admin-panel.tsx`，整页 `fixed inset-0` 覆盖在工作台之上（开着时锁 `body` 滚动），Esc 或「返回工作台」退出。
**沿用同一套版面语言**，不另起一套后台皮肤：无衬线抬头 + 1px 墨黑主线 + 铅字条（计价/时区/流水上限/取数时刻）+ 1px 副线。

- 汇总数用 30px 衬线 `tabular-nums`，小标签 `text-xs uppercase tracking-[0.14em]`；失败数为 0 时不上洋红
- 身份筛选是一排文字按钮（当前项墨黑 + 下划线，其余深青），**汇总数、折线、流水三处同时跟着筛**
- 两张表都是裸表格：表头 1px 墨黑下框线、行间 1px `#E0DDDD`，数字右对齐 `tabular-nums`，不做斑马纹与卡片
- 折线图**手绘 SVG**，不引图表库：只需两条 1px 折线 + 一条基线，
  图表库的默认卡片/圆角/阴影/彩色调色板反而要一路改回来。灰线=派发、青线=成功出图（仍然只有两个强调色）；
  纵轴三档刻度（0/半程/峰值，峰值很小时去重），横轴只标首/中/尾三个日期；
  每天一条透明热区，鼠标扫过在图上方读出「日期 · 派发 · 成功 · 金额」——读数用文字行，不做浮层 tooltip 卡片

## 交互与状态

- 主按钮常显「开始批量生成」（一生三时「开始生成（一生三）」），运行中转「正在生成…」并禁用；
  「取消批次」常显，非运行态置灰——两个按钮位置固定，不互相替换
- **用户选择是强制的**：未选中时选择器走**洋红描边 + 洋红字**（待办信号，占位文案「选择用户身份」）；选中后转墨黑描边。
  未选择时点生成弹居中 AlertDialog「请选择员工身份」，**弹窗内平铺五个身份按钮**（奇数个时最后一个占满整行，不留半格空位），
  点选即记住并立即以该身份开始生成——不做「弹窗 → 再去拉下拉」的两级跳，
  也不提供"以默认身份继续"（上游无 Token 直接 401）
- **任务卡片五态**，靠边框语义区分，统一三段式（编号 / 状态 / 动作），不显示提示词全文（挂在 `title` 上）：
  - 排队：虚线灰边框 + 「排队中 · 队列前方 N 张」/「等待重试 · 第 N 次」+ 移出队列
  - 生成中：1px 青边框 + `bg-raised` + `ink-pulse` 信号点 + `已耗时 mm:ss · 无进展剩余 mm:ss · 第 N/M 次`
    + 2px 进度条；**剩余不足 15% 时槽与填充整体转洋红**
  - 成功：`halftone` 样张满铺 + 左上墨块编号（方角、无圆角）+ 悬停浮出墨色操作层
  - 失败：1px 洋红边框 + `bg-rose-wash` + 等宽 `TIMEOUT` / `FAILED` 钢印 + 「超时未出图」/「上游拒绝」+ 重试此张
  - 已中断：虚线灰边框 + `CANCELLED` 钢印 + 「由操作员手动停止」+ 继续生成（青字）
- 成功卡悬停层：墨色渐变 + 提示词 + 四个纸字铅块（下载 / 复制链接 / 放大 / 重新生成）+ 本张耗时；
  操作即时反馈用文案切换（`已复制` / `下载中`），不用图标
- 单点操作一律就地（卡片内），不弹二次确认——中断与重试都是可逆的
- 批量下载点击「全部下载」即开始，并同步弹居中 AlertDialog 提示：如下载无法进行，检查浏览器左上角地址栏
  站点权限中的「自动下载多个文件」；按钮「知道了」，不阻塞下载
- 拆分模型不可用时弹 AlertDialog 询问是否「本地分条继续」，不静默降级
- 提醒类弹窗一律 AlertDialog 居中，`sm:max-w-sm`、`bg-raised` + 墨黑描边、标题无衬线 15px、正文 13px 次色
- 空态：虚线框内一句状态（17px 次色）+ 一句指引（13px 弱色），不放假图、不放图标
- 「预计剩余」按已落地任务的平均墙钟时间推算，样本不足或已收工时**不显示**——不编造进度
- 上游延迟是**探活请求的实测往返时间**，探测失败时整段隐藏——不写死数字
- 历史记录：localStorage 存最近 10 次运行（含结果），左栏底部列表可回放（`今日 / 昨日 / M-D` + 时刻），
  回放后仍可对单张重新生成
- 分步提交规范化：每条提示词内部换行合并为空格；提交给上游时单条加 `1.` 编号，把长提示词钉成"一条"，
  避免拆分 AI 二次拆分。外部编号文本（历史回放 / 示例模板）自动解析回填分步框，无编号原文自动切换统一模式
- 一生三：同一条提示词的多份用 `#01-1 / #01-2 / #01-3` 编号，相邻排列，靠编号而非分组容器区分
- 图片下载必须 fetch + blob（跨域签名 URL 下 `<a download>` 无效）
- 图片上的浮层（编号墨块、悬停操作层、灯箱底图区）允许深色不透明/半透明——媒体内容上保持可读性

## 设计禁忌清单

- 禁止紫蓝渐变、玻璃拟态、发光特效、大圆角（一律 2px）
- 禁止图标——包括 lucide 线性图标；动作用文字，状态用颜色与线条
- 禁止引入第三个强调色，尤其不要加成功绿
- 禁止 Hero 大标题营销式首屏——这是印务台，进页面即工作区
- 禁止表情符号当图标
- 禁止装饰性插图与无意义留白
- 禁止厚重投影与多层卡片套娃
- 禁止用内部滚动条切分页面——整页滚动 + 左栏 sticky
