# DESIGN.md — 云悦AI工作台（JoyCloud）

## 产品定位与意象

批量图片生成工作流的可视化工作台。不是 AI 玩具，是生产工具。

**意象锚点**：明亮行政办公室里的生产管理台——白纸、活页夹、钢笔，一切井井有条。参考气质：Stripe Dashboard / Linear 浅色模式 / 飞书管理后台，干净、克制、专业、可信赖，信息密度高、装饰极少。

**明确绕开的第一直觉**：商务浅色泛滥的"惨白底 + 高饱和亮蓝 + 厚重投影卡片"，一律禁止；也禁止 AI 产品俗套的紫蓝渐变。

## Design Tokens

### 色彩
- 页面底：`#F5F6F8`（极浅冷灰，纸堆感，不用纯白惨白）
- 面板/卡片：`#FFFFFF`，悬浮层 `#FFFFFF`
- 边框：`#E4E7EC`（发丝级 1px 分割线，层次靠边框与底色差，不堆投影）
- 主文字：`#1C2128`（墨水瓶近黑），次文字 `#59636E`，弱文字 `#98A2AE`
- 主色（企业蓝）：`#2563EB`——全站唯一高饱和色，仅用于主按钮、运行中状态、关键数据、选中态
- 成功绿：`#16A34A`；失败红：`#DC2626`；均功能性使用，不做装饰
- 禁止：任何紫/蓝渐变、彩色发光描边、厚阴影

### 字体
- UI 与中文：Inter + 系统中文栈（PingFang SC / Microsoft YaHei）
- 数据、编号、run_id、计时、计数：等宽字体 `ui-monospace, "JetBrains Mono", Menlo, monospace`
- 排版节奏：小字号高密度（13–14px 正文），标题不超过 15px，靠字重与字间距分层，不靠大字号

### 形状与线条
- 圆角：面板 8px，控件 6px，标签 4px；禁止大圆角胶囊（状态点除外）
- 图标：lucide 线性图标，16px 为主，stroke 1.5
- 分割一律 1px 边框 + 底色差，不用投影堆叠层次

### 动效
- 快速功能化：150–250ms `ease-out`，禁止弹性/漂浮动画
- 运行中节点：企业蓝信号点脉冲呼吸（`signal-pulse` 1.6s）
- 图片下线感：结果按 index 依次 stagger 入场（fade + translateY，间隔 40ms）
- 骨架态：浅灰 shimmer，不闪烁刺眼

## 布局

- 顶栏 52px 白底：品牌字标 + 服务健康状态点 + 用户选择
- 左侧固定输入面板 400px：设备的"进料口"——顶部「分步生成 / 统一生成」分段切换（灰底槽 + 企业蓝选中态，默认分步）；分步模式为编号文本框列表（初始 5 框、下方虚线 + 按钮追加、单框可删/可复制，列表超高内滚动），统一模式为单一大文本域；下方「每条生成份数」分段切换（1 份 / 3 份·一生三）；附示例模板、生成/取消主按钮
- 右侧主区：批次追踪条（进度条 + 成功/失败/中断/生成中/排队计数 + 并发与超时展示 + 批量操作 + 日志开关）+ 任务网格
- 任务网格：`grid-cols-2 → 5`，1:1 卡片；成功卡 hover 浮现操作层（下载 / 复制链接 / 放大 / 重新生成）
- 移动端：输入面板折叠为顶部抽屉，网格 2 列

## 交互与状态

- 主按钮两态：`开始批量生成`（企业蓝实底白字，一生三时文案变「开始生成（一生三）」）→ `取消批次 / 取消拆分`
- **用户选择是强制的**：右上角占位文案「请选择用户」，未选中时选择器整体走信号蓝描边 + 蓝字（唯一高饱和色，用作"待办"信号，不引入第二种强调色）；未选择时点击生成弹居中 AlertDialog，**弹窗内平铺四个用户按钮**，点选即记住并立即开始生成——不做「弹窗 → 再去拉下拉」的两级跳，也不提供"以默认身份继续"（上游无 Token 直接 401）
- **任务卡片五态**，靠边框语义区分，不加新颜色：
  - 排队：虚线灰边框 + 「排队中」/「等待重试 · 第 N 次」+ 移出队列
  - 生成中：企业蓝细边框 + 脉冲信号点 + 已耗时 / 超时剩余倒计时 + 1.5px 进度条 + 「第 N/M 次」+ 「中断此张」；剩余不足 15% 时进度条与倒计时转失败红
  - 成功：图片满铺 + 左上编号角标
  - 失败：红边框 + `FAILED` / `TIMEOUT` 标识 + 错误文案 + 「重试此张」
  - 已中断：虚线灰边框 + `CANCELLED` + 「继续生成」
- 单点操作一律就地（卡片内），不弹二次确认——中断与重试都是可逆的
- 批量下载点击「全部下载」即开始，并同步弹居中 AlertDialog 提示：如下载无法进行，检查浏览器左上角地址栏站点权限中的「自动下载多个文件」；按钮「知道了」，不阻塞下载
- 拆分 AI 不可用时弹 AlertDialog 询问是否「本地分条继续」，不静默降级
- 提醒类弹窗一律用 AlertDialog 居中呈现，`sm:max-w-sm`、标题 14px、正文 12px 弱色，按钮 32px 高小字，不用营销式插画
- 空态：网格区显示虚线框占位与指引文案，不放假图
- 历史记录：localStorage 存最近 10 次运行（含结果），左栏底部列表可回放；回放后仍可对单张重新生成
- 分步提交规范化：分步模式每条提示词内部换行合并为空格；提交给上游时单条加 `1.` 编号，把长提示词钉成"一条"，避免拆分 AI 二次拆分。外部编号文本（历史回放 / 示例模板）自动解析回填分步框，无编号原文自动切换统一模式
- 一生三：同一条提示词的多份用 `#01-1 / #01-2 / #01-3` 编号，相邻排列，靠编号而非分组容器区分——不引入额外的卡片套娃层级
- 图片下载必须 fetch + blob 方式（跨域签名 URL 下 `<a download>` 无效）
- 图片上的浮层（序号徽章、灯箱底图区）允许深色半透明——媒体内容上保持可读性

## 设计禁忌清单

- 禁止紫蓝渐变、玻璃拟态、发光特效、大圆角
- 禁止 Hero 大标题营销式首屏——这是工具台，进页面即工作区
- 禁止表情符号当图标
- 禁止装饰性插图与无意义留白
- 禁止厚重投影与多层卡片套娃
